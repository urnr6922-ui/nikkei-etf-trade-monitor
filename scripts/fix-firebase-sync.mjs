// Firebase cloud-sync injection is intentionally disabled while the app runtime is stabilized.
// Market-data fetching, chart rendering, indicators, and multi-device market-data updates do not depend on Firebase.
console.log('Firebase sync injection disabled for runtime stability')
